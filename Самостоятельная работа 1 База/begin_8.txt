#include <iostream>
using namespace std;

int main() {
    float a, b;
    cout << "Enter two numbers a and b: ";
    cin >> a >> b;
    float avg = (a + b) / 2;
    cout << "The arithmetic mean of a and b is " << avg << endl;
    return 0;
}
